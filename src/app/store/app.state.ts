// app.state.ts

import { User } from './../models/user.model'; // Import any necessary models

export interface AppState {
  user: User; // Example: Assuming you have a User model representing user data
  // Add other properties representing different parts of your application state
}
